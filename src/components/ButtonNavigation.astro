---
interface Props {
	links: string;
    title: string;
}

const { links, title } = Astro.props;
---

<a href={links} class="px-4 w-fit flex items-center font-bold border-b-2 border-black/30 hover:border-dark-blue transition-all duration-100 ">
    {title}
</a>