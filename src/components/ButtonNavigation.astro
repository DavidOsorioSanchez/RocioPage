---
interface Props {
	links: string;
    title: string;
    selected: boolean;
}

const { links, title, selected } = Astro.props;
---

<a href={links} class={`px-4 w-fit hidden items-center font-bold border-b-2 ${selected ? "border-blue" : "border-black/30"} text-nowrap hover:border-dark-blue transition-all duration-100 min-[685px]:flex`}>
    {title}
</a>