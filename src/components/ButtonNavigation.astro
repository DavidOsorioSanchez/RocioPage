---
interface Props {
	links: string;
    title: string;
}

const { links, title } = Astro.props;
---

<a href={links} class="px-4 w-fit hidden items-center font-bold border-b-2 border-black/30 text-nowrap hover:border-dark-blue transition-all duration-100 min-[560px]:flex">
    {title}
</a>